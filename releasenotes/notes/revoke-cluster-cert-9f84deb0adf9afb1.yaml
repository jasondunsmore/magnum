---
features:
  - Add microversion 1.4 to support a revocation of a cluster's CA
    certificate.  This gives admins a way to restrict/deny access to
    an existing cluster once a user has been granted access.
