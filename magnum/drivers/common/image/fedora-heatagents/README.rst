=================
fedora-heatagents
=================

To generate a Fedora image with Docker and the Heat agents, install
build dependencies as documented in the parent directory and then run
the ``./build.sh`` script.
